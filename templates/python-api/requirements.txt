# كل شيء اختياري — المشروع يعمل على المكتبات القياسية فقط.
# فكّ التعليق عند الحاجة فقط (التنزيل على الموبايل يستهلك بيانات):
# fastapi==0.115.*
# uvicorn[standard]==0.30.*
# httpx==0.27.*         # اختبارات
# pytest==8.*           # اختبارات
# ruff==0.6.*           # تنسيق وفحص تلقائي
